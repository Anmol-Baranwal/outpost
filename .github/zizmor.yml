rules:
  dangerous-triggers:
    ignore:
      # notify-pr.yml: pull_request_target for Slack notifications on fork PRs.
      # Does NOT checkout PR code. permissions: {} or minimal scopes.
      - notify-pr.yml
