# Discord bot token used ONLY by this MCP server (Community Signals workflow).
# Distinct from DISCORD_TOKEN used by apps/discord-bot — keep them separate so
# the two bots can have different perms / different identities if needed.
DISCORD_MCP_TOKEN=your_community_signals_bot_token_here
