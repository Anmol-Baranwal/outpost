{
    "name": "@copilotkit/outpost-docs",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "npx serve . -l 4000",
        "build": "echo 'Static site — no build step required'"
    }
}
