# Railway config — outpost-slack-bot (background worker, no public port)
# In Railway: set this service's "Config file path" to apps/slack-bot/railway.toml
# and leave Root Directory empty (build context must be the repo root).

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/slack-bot/Dockerfile"

[deploy]
restartPolicyType = "ALWAYS"
